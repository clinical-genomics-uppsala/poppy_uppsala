Cluster and workstation resource overrides for this pipeline should live in this directory.

The current flat resources file is kept during the migration, but the shared layout expects site-specific resource YAMLs to be grouped here.
